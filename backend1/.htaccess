<IfModule mod_rewrite.c>
    RewriteEngine On

    # Force all requests through the public directory
    RewriteCond %{REQUEST_URI} !^/public/
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>

# Prevent directory listings if overrides are respected
Options -Indexes
